import { AddUnderscoreDirective } from './add-underscore.directive';

describe('AddUnderscoreDirective', () => {
  it('should create an instance', () => {
    const directive = new AddUnderscoreDirective();
    expect(directive).toBeTruthy();
  });
});
